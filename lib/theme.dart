import 'package:flutter/material.dart';

class AppColors {
  // These match the colors used in your screens
  static const Color primaryBlue = Color(0xFF3E84A8); 
  static const Color primaryGreen = Color(0xFF4ACED0);
  static const Color darkButton = Color(0xFF1E1E1E);
  
  // Adding the Home Screen colors for consistency
  static const Color late = Color(0xFFE74C3C);
  static const Color text = Color(0xFF1E1E1E);
  static const Color subText = Color(0xFF6B7280);
}